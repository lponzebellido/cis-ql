#ifndef TOKEN_H
#define TOKEN_H

#include <string>

enum class TokenType {
    FIND, SEARCH, EXTRACT, MATCH, COUNT, LOAD, SAVE, USE, EXPORT, DEFINE,
    MOTIF, MODULE, GENE, SEQUENCE, ANNOTATION, TRACK, REGION, PROMOTER, PROMOTERS, ENHANCER,
    OF,
    UPSTREAM, DOWNSTREAM, WITHIN, WITH, FROM, AT, TO, FORMAT,
    SPACING, ORDER, ORIENTATION, ANY, SAME, OPPOSITE, AS_WRITTEN,
    EVIDENCE, ACCESSIBILITY, BINDING, OTHER, ASSAY, SAMPLE, CONDITION,
    REPLICATE, CONTROL,
    TRACK_SCORE, SIGNAL_VALUE, MINUS_LOG10_PVALUE,
    MINUS_LOG10_QVALUE, EVIDENCE_CLASS, SUPPORT_COUNT,
    BED, NARROWPEAK, GFF3, TSV,
    BP, KB, MB,
    AND, OR, NOT, WHERE, LENGTH, START, END, MOD, AS,
    EXON, INTRON, UTR, TSS, CDS,
    CHR, CHROMOSOME, STRAND, POSITIVE, NEGATIVE,
    INTERSECT, UNION, EXCEPT, OVERLAPS, NEAR, CONSENSUS, ANCHOR, MIN_SUPPORT,
    MIN_RECIPROCAL_OVERLAP, MAX_SUMMIT_DISTANCE,
    SIMILARITY, REVERSE_COMPLEMENT,
    MATRIX, SCAN, THRESHOLD, PVALUE, QVALUE, BACKGROUND, UNIFORM,
    ANALYZE, GC_CONTENT, CPG_ISLANDS, WINDOW,
    IF, THEN, ELSE, ENDIF,
    FOREACH, IN, DO, ENDFOR,

    ID,
    NUM,       
    FLOAT,     
    STRING,

    SEMICOLON, 
    COMMA,     
    ASSIGN,    
    GREATER,   
    LESS,      
    GREATER_EQ,
    LESS_EQ,   
    LPAREN,    
    RPAREN,    
    LBRACKET,
    RBRACKET,
    PERCENT,   

    END_OF_FILE,
    UNKNOWN,
    ERROR_TOKEN
};

struct Token {
    TokenType type;
    std::string lexeme;
    int line;
    int column;
};

std::string tokenTypeToString(TokenType type);

#endif
